linters-settings:
  errcheck:
    exclude-functions:
      - (*github.com/jackc/pgx/v5.Tx).Rollback
    check-blank: false
  staticcheck:
    checks: ["all"]

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
